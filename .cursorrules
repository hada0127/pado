// Pado .cursorrules

// Folder structure
const folderStructure = `
pado/
  pado.ts
  eslint/
    eslintDevPlugin.ts
  vite/
    devPlugin.ts
    makeCaches.ts
    getHtmlFromCache.ts
    utils.ts
  settings/
    publicMimeTypes.ts
src/
  index.html
  app/
    page.module.scss
    page.pado
    page.ts
  styles/
    global.scss
    mixins.scss  
    `;

// Additional instructions
const additionalInstructions = `

1. Use TypeScript for type safety
2. Use SCSS Modules for styling
3. Implement proper error boundaries
4. Use environment variables for configuration
5. Use type aliases instead of interfaces for type declarations.
6. When modifying existing types, extend from the original type.
7. Declare types in type files unless they are component-specific.
8. Lets verify steb by step 
`;
